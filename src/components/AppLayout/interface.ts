import React from 'react'
import { FunctionComponentType } from 'src/interfaces/common/FunctionComponentType'

export interface AppLayoutProps extends FunctionComponentType {
  title?: React.ReactNode
}
